using System;

namespace quotesAndStrings
{
    class quotesAndStrings
    {
        static void Main(string[] args)
        {
            string quotesTest = "The \"use\" of quotations causes difficulties.";
            string anotherTest = "The use of quotations causes difficulties.";

            Console.WriteLine(quotesTest);
            Console.WriteLine(anotherTest);
        }
    }
}
